# Generated by `dagger develop`. Do not commit — they are reproduced from
# dagger.json on demand and would otherwise lock to a specific engine version
# that may not match what a contributor has installed locally.
internal/dagger/
dagger.gen.go

# Go build artefacts.
*.exe
*.test
*.out
/bin/
/dist/
/vendor/
go.work
go.work.sum

# go.sum IS committed — it locks transitive dep versions. go.mod + go.sum
# together give reproducible builds. (Only `internal/dagger/` and
# `dagger.gen.go` above are truly generated-per-engine-version.)

# macOS
.DS_Store
**/.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Local dagger cache
.dagger/
/internal/querybuilder
/internal/telemetry
/.env
